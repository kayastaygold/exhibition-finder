# 展覽查詢網站 — 資料規格 (schema.md)

## 1. 資料源

文化部「文化資料開放服務網」— 展覽資訊資料集

| 項目 | 內容 |
|---|---|
| 提供單位 | 文化部 |
| 授權 | 政府資料開放授權條款-第1版（甲類資料，免費、可再轉授權） |
| 計費 | 免費 |
| 更新頻率 | 每日 |
| 資料集更新日期 | 2026/05/28 |
| 編碼 | UTF-8 |

### 端點

```
JSON: https://cloud.culture.tw/frontsite/trans/SearchShowAction.do?method=doFindTypeJ&category=6
XML:  https://cloud.culture.tw/frontsite/trans/SearchShowAction.do?method=doFindTypeX&category=6
```

`category` 參數決定活動類別，同一組欄位規格：

| category | 類別 |
|---|---|
| `6` | 展覽資訊 |
| `7` | 講座資訊 |
| `11` | 綜藝活動 |
| `15` | 其他藝文活動 |
| `19` | 文化部研習課程 |
| `all` | 藝文活動—所有類別 |

> 建議：先只做 `category=6`（展覽）把流程跑通，之後再擴充。`all` 資料量大很多。

---

## 2. 官方欄位清單

以下為文化部公告的欄位說明（原文照錄）：

**活動層級**

| 欄位 | 說明 | 備註 |
|---|---|---|
| `version` | 發行版本 | |
| `UID` | 唯一辨識碼 | 主鍵 |
| `titile` | 活動名稱 | ⚠️ 官方欄位名就是這個拼字（少一個 t），不是 `title` |
| `category` | 活動類別 | |
| `showUnit` | 演出單位 | |
| `descriptionFilterHtml` | 簡介說明 | 含 HTML，需消毒 |
| `discountInfo` | 折扣資訊 | |
| `imageURL` | 圖片連結 | |
| `masterUnit` | 主辦單位 | |
| `subUnit` | 協辦單位 | |
| `supportUnit` | 贊助單位 | |
| `otherUnit` | 其他單位 | |
| `webSales` | 售票網址 | |
| `sourceWebPromote` | 推廣網址 | |
| `comment` | 備註 | |
| `editModifyDate` | 編輯時間 | |
| `sourceWebName` | 來源網站名稱 | |
| `startDate` | 活動起始日期 | 時間篩選主要依據 |
| `endDate` | 活動結束日期 | 時間篩選主要依據 |
| `hitRate` | 點閱數 | 可用來排序熱門度 |
| `showinfo` | 活動場次資訊 | 陣列，見下 |

**場次層級（`showinfo` 陣列內）**

| 欄位 | 說明 | 備註 |
|---|---|---|
| `time` | 單場次演出時間 | |
| `endTime` | 結束時間 | |
| `location` | 地址 | 地點篩選主要依據 |
| `locationName` | 場地名稱 | |
| `latitude` | 緯度 | 距離計算用 |
| `longitude` | 經度 | 距離計算用 |
| `onSales` | 是否售票 | |
| `Price` | 售票說明 | ⚠️ 首字大寫 |

---

## 3. 結構重點

**地點資訊在場次層級，不在活動層級。** 一個展覽可以有多個 `showinfo` 場次，各自有自己的地址與經緯度（巡迴展就是這種情況）。

這對資料模型有直接影響：

- 若以「活動」為單位，一筆資料可能對應多個地點 → 地點篩選要判斷「任一場次符合」
- 若以「場次」為單位攤平（flatten），篩選邏輯單純很多，但同一展覽會重複出現 → 顯示時要去重

**建議採用攤平後再分組的做法**：抓取時展開成場次清單，篩選在場次層級做，回傳結果再依 `UID` 收攏成活動卡片，卡片上標示符合條件的場次。

---

## 4. 已知風險（需在第一次實際抓取時確認）

| # | 風險 | 影響 | 處理方向 |
|---|---|---|---|
| 1 | **CORS** — 政府 API 通常不帶 `Access-Control-Allow-Origin` 標頭 | 瀏覽器直接 `fetch` 會被擋，這是最可能第一個撞到的牆 | 改成建置階段抓取，產生靜態 JSON 給前端讀 |
| 2 | **無查詢參數** — 這支 API 似乎不支援依日期或地區過濾，一次回傳全部 | 資料量大，前端直接載入會很慢 | 同上：預處理 + 瘦身欄位 |
| 3 | `latitude` / `longitude` 常為**空字串**而非 `null` | `parseFloat("")` 得到 `NaN`，地圖或距離計算會爆 | 統一正規化：空字串、`"0"`、非數字一律轉 `null`，並保留「無座標」的活動用地址文字比對 |
| 4 | `startDate` / `endDate` 格式 | 篩選邏輯的根基 | 確認是 `YYYY/MM/DD` 還是 `YYYY-MM-DD`；是否有空值、是否有 `endDate` 早於 `startDate` 的髒資料 |
| 5 | `descriptionFilterHtml` 含 HTML 標籤 | XSS 風險 + 版面破壞 | 消毒後再渲染，或只取純文字前 N 字當摘要 |
| 6 | `location` 地址格式不一 | 縣市篩選不可靠 | 用正則抽出「○○縣/市」；抽不出來的 fallback 用經緯度反查 |
| 7 | 已結束的活動可能仍在資料中 | 使用者看到過期展覽 | 預設過濾 `endDate < today` |
| 8 | `UID` 是否真的唯一 | 去重邏輯 | 抓取後檢查重複數量 |

---

## 5. 建議架構

```
[GitHub Actions 每日排程]
        ↓
  抓取 API → 清洗 → 攤平 → 輸出 events.json（靜態檔）
        ↓
  [前端] 載入 events.json → 依地點/日期在瀏覽器端篩選
        ↓
  GitHub Pages 免費託管
```

這個做法一次解決 CORS、速度、以及「不想管後端」三件事。前端不需要伺服器，純靜態即可。

---

## 6. 篩選邏輯定義

**時間篩選** — 判斷活動期間與查詢期間是否重疊：

```
活動符合 = (startDate <= 查詢結束日) AND (endDate >= 查詢開始日)
```

常用預設選項：今天／本週／本月／自訂區間。

**地點篩選** — 兩種模式：

1. **縣市** — 從 `location` 抽出縣市字串比對。無座標的活動也適用，覆蓋率高。
2. **距離** — 以使用者座標為中心，用 Haversine 公式算距離並依半徑過濾。精準，但缺座標的活動會被排除。

建議兩種都做，預設用縣市（覆蓋率優先），距離當進階選項。

---

## 7. 下一步待辦

- [ ] 實際抓一次 API，存成 `sample.json`
- [ ] 驗證上表第 3、4、6、8 項的實際狀況
- [ ] 統計：總筆數、有座標的比例、各縣市分布、日期格式樣態
- [ ] 確認 `showinfo` 的實際巢狀結構與欄位大小寫
- [ ] 依實際結果修正本文件，再開始寫前端
