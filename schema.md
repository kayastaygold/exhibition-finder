# 展覽查詢網站 — 資料規格 (schema.md)

## 1. 資料源

文化部「文化資料開放服務網」— 展覽資訊資料集

| 項目 | 內容 |
|---|---|
| 提供單位 | 文化部 |
| 授權 | 政府資料開放授權條款-第1版（甲類資料，免費、可再轉授權） |
| 計費 | 免費 |
| 更新頻率 | 每日 |
| 資料集更新日期 | 2026/05/28 |
| 編碼 | UTF-8 |

### 端點

```
JSON: https://cloud.culture.tw/frontsite/trans/SearchShowAction.do?method=doFindTypeJ&category=6
XML:  https://cloud.culture.tw/frontsite/trans/SearchShowAction.do?method=doFindTypeX&category=6
```

`category` 參數決定活動類別，同一組欄位規格：

| category | 類別 |
|---|---|
| `6` | 展覽資訊 |
| `7` | 講座資訊 |
| `11` | 綜藝活動 |
| `15` | 其他藝文活動 |
| `19` | 文化部研習課程 |
| `all` | 藝文活動—所有類別 |

> 建議：先只做 `category=6`（展覽）把流程跑通，之後再擴充。`all` 資料量大很多。

---

## 2. 官方欄位清單

> ✅ **已依實際抓取結果修正**（2026/09/07，`sample.json`，N=344，`category=6`）。原始官方文件的欄位拼字與型別跟實際回傳**不完全一致**，下表已改為實測結果，官方文件的說法用刪除線標記於備註欄。

**活動層級**（344 筆皆有以下全部欄位，出現率 100%，除非備註另外標示）

| 欄位 | 說明 | 型別（實測） | 備註 |
|---|---|---|---|
| `version` | 發行版本 | string | 樣本中全部為 `"1.4"` |
| `UID` | 唯一辨識碼 | string | 主鍵，344 筆皆唯一，無重複（見 4-8） |
| `title` | 活動名稱 | string | ⚠️ 實測拼字是正確的 `title`，~~官方文件寫的 `titile`~~ 在樣本中未出現 |
| `category` | 活動類別 | string | 樣本中全部為 `"6"` |
| `showUnit` | 演出單位 | string | |
| `descriptionFilterHtml` | 簡介說明 | string | 樣本 344 筆中**沒有一筆**含 HTML 標籤（見 4-5，風險等級下修） |
| `discountInfo` | 折扣資訊 | string | 83%（284/344）為空字串 |
| `imageUrl` | 圖片連結 | string | ⚠️ 實際欄位名是 `imageUrl`（非官方文件的 `imageURL`）；94.8%（326/344）為空字串，前端需準備預設圖 |
| `masterUnit` | 主辦單位 | **array\<string\>** | ⚠️ 是陣列不是字串！8.1%（28/344）為空陣列 `[]` |
| `subUnit` | 協辦單位 | **array\<string\>** | 88%（302/344）為空陣列 |
| `supportUnit` | 贊助單位 | **array\<string\>** | 99%（341/344）為空陣列 |
| `otherUnit` | 其他單位 | **array\<string\>** | 75%（258/344）為空陣列，常見值如「(指導)文化部」 |
| `webSales` | 售票網址 | string | 94%（324/344）為空字串 |
| `sourceWebPromote` | 推廣網址 | string | 41%（142/344）為空字串 |
| `comment` | 備註 | string | 93%（321/344）為空字串 |
| `editModifyDate` | 編輯時間 | string | 94%（324/344）為空字串；有值時格式為 `YYYY/MM/DD HH:mm:ss` |
| `sourceWebName` | 來源網站名稱 | string | 皆非空 |
| `startDate` | 活動起始日期 | string | 時間篩選主要依據；格式見 4-4 |
| `endDate` | 活動結束日期 | string | 時間篩選主要依據；格式見 4-4 |
| `hitRate` | 點閱數 | **number (int)** | 可用來排序熱門度；非字串 |
| `showInfo` | 活動場次資訊 | array\<object\> | ⚠️ 實際欄位名是 `showInfo`（首字大寫 I，非官方文件的 `showinfo`）。陣列，見下 |

**場次層級（`showInfo` 陣列內）**

| 欄位 | 說明 | 型別（實測） | 備註 |
|---|---|---|---|
| `time` | 單場次演出時間 | string | 格式 `YYYY/MM/DD HH:mm:ss`，344/344 一致 |
| `endTime` | 結束時間 | string | 格式同上 |
| `location` | 地址 | string | 地點篩選主要依據；格式不一致，見 4-6 |
| `locationName` | 場地名稱 | string | 皆非空 |
| `latitude` | 緯度 | string \| **null** | ⚠️ 不是空字串，是 JSON `null`（見 4-3）；有值時為字串，如 `"24.157234"` |
| `longitude` | 經度 | string \| **null** | 同上 |
| `onSales` | 是否售票 | string | ⚠️ 三種值：`"N"`(261) / `"Y"`(80) / `"UNKNOWN"`(3)，不是純布林 Y/N |
| `price` | 售票說明 | string | ⚠️ 實際欄位名**全小寫** `price`（非官方文件的 `Price`）；81%（278/344）為空字串 |

---

## 3. 結構重點

**地點資訊在場次層級，不在活動層級。** 一個展覽可以有多個 `showInfo` 場次，各自有自己的地址與經緯度（巡迴展就是這種情況）。

> ✅ **實測補充**：在 `category=6`（展覽）、N=344 的樣本中，**每一筆 `showInfo` 陣列長度都恰好是 1**（344/344），沒有觀察到單一 `UID` 對應多場次/多地點的情況。這代表巡迴展的多場次結構在展覽類別中可能罕見或不存在——但因為 `all` 類別（講座、綜藝活動等）尚未驗證，且官方欄位本來就設計成陣列，資料模型仍應**保留陣列處理能力**，不能寫死成「每筆活動只有一個地點」。

這對資料模型有直接影響：

- 若以「活動」為單位，一筆資料**理論上**可能對應多個地點 → 地點篩選要判斷「任一場次符合」
- 若以「場次」為單位攤平（flatten），篩選邏輯單純很多，即使目前樣本中攤平後跟原始筆數相同（344→344），程式邏輯仍應通用化以應付未來多場次資料

**建議採用攤平後再分組的做法**：抓取時展開成場次清單，篩選在場次層級做，回傳結果再依 `UID` 收攏成活動卡片，卡片上標示符合條件的場次。

---

## 4. 已知風險（已於 2026/09/07 用實際樣本驗證，N=344，`category=6`）

| # | 風險 | 驗證結果 | 影響 | 處理方向 |
|---|---|---|---|---|
| 1 | **CORS** — 政府 API 通常不帶 `Access-Control-Allow-Origin` 標頭 | 未實測（本階段用伺服器端抓取，非瀏覽器 fetch，無法驗證） | 瀏覽器直接 `fetch` 會被擋，這是最可能第一個撞到的牆 | 改成建置階段抓取，產生靜態 JSON 給前端讀 |
| 2 | **無查詢參數** — 這支 API 似乎不支援依日期或地區過濾，一次回傳全部 | ✅ 確認：`category=6` 一次回傳全部 344 筆，無分頁/篩選參數痕跡 | 資料量大，前端直接載入會很慢（本次樣本約 544KB） | 同上：預處理 + 瘦身欄位 |
| 3 | `latitude` / `longitude` 常為空字串而非 `null` | ❌ **與實測不符，已修正**：樣本中**沒有任何一筆是空字串 `""`**。無座標時的值是 JSON **`null`**（229/344，66.6%），有值時是字串，如 `"24.157234"`（115/344，33.4%，且全部落在合理經緯度範圍、非 0）。原描述「空字串」是錯的，但「無座標比例高」的結論成立 | `parseFloat(null)` 得到 `NaN`，地圖或距離計算一樣會爆 | 正規化：`null`、空字串、非數字、`"0"` 一律轉 `null`；66.6% 無座標的活動必須靠 `location` 文字做地點篩選，不能只靠座標 |
| 4 | `startDate` / `endDate` 格式 | ✅ 確認格式：**344/344 皆為 `YYYY/MM/DD`**（例：`2026/09/08`），無 `YYYY-MM-DD` 混用、無空值。`showInfo.time` / `endTime` 則是 `YYYY/MM/DD HH:mm:ss`（344/344 一致）。抽查全部 344 筆：**沒有 `endDate < startDate` 的髒資料**，日期皆可解析 | 篩選邏輯的根基 | 統一用 `YYYY/MM/DD` 解析；雖然本樣本乾淨，程式仍應對解析失敗做防呆（fallback 為「日期未知」而非拋錯） |
| 5 | `descriptionFilterHtml` 含 HTML 標籤 | ⚠️ **風險等級下修**：樣本 344 筆中，**沒有一筆**偵測到 HTML tag（`<tag>` 模式比對 0 命中）。內容看起來是純文字（含 `\r\n` 換行符） | 原本擔心的 XSS/版面破壞在本樣本未出現 | 仍建議保留消毒（`\r\n` → 需轉換成 `<br>` 或用 `white-space: pre-line`），不可假設未來资料一定無標籤，通過 sanitize 函式渲染是安全底線 |
| 6 | `location` 地址格式不一 | ✅ 確認且找到具體缺口：用「臺北市／新北市／...」等 26 個縣市關鍵字比對，**95.6%（329/344）可抽出縣市**，county 分布見下表。**剩餘 4.4%（15/344）地址完全沒有縣市前綴**（例：`"中正區羅斯福路四段40巷1-2號一樓"`），且這 15 筆**同時也沒有經緯度**（`latitude` 皆為 `null`）——縣市正則 + 經緯度反查兩種 fallback 都失效，是真正的資料缺口 | 縣市篩選漏掉 4.4% 的活動 | 用正則抽出「○○縣/市」；對於這 15 筆真正雙缺的資料，唯一可行的 fallback 是用 `locationName`（場地名稱，如「谷公館」「安卓藝術」）做人工/半人工的場館→縣市對照表，或標示為「地點未知」讓使用者知道存在但篩選不到 |
| 7 | 已結束的活動可能仍在資料中 | ❌ **與猜測不符，已修正**：以今天 2026/09/07 為基準逐筆核對 344 筆，**`endDate < today` 的筆數是 0**。API 本身似乎已經在伺服器端過濾掉已結束的活動，只回傳「進行中／未來」的展覽（88.1% 進行中、11.9% 未來未開展）。見「API 回傳範圍結論」 | 目前資料中沒有過期展覽，這項風險在本樣本未發生 | 仍建議前端保留 `endDate < today` 的防呆過濾，因為不保證 API 永遠即時更新 |
| 8 | `UID` 是否真的唯一 | ✅ **確認唯一**：344 筆資料 344 個不重複 `UID`，重複數 = 0 | 去重邏輯 | 可安心以 `UID` 當主鍵；但因抓取頻率是每日，`UID` 跨日是否穩定仍建議之後多次抓取後比對 |
| 9（新增） | 官方文件的欄位名稱/大小寫與實際不符 | ✅ 確認 3 處落差：`title`（非 `titile`，文件寫錯）、`showInfo`（非 `showinfo`，I 大寫）、`imageUrl`（非 `imageURL`）、`price`（非 `Price`，全小寫）| 若照文件欄位名寫死解析程式會全部讀到 `undefined` | 一律以本文件第 2 節「型別（實測）」欄為準，不要照抄官方文件的拼字 |
| 10（新增） | `masterUnit` / `subUnit` / `supportUnit` / `otherUnit` 是**陣列**不是字串 | ✅ 確認：型別為 `array<string>`，例如 `["國立自然科學博物館"]`；無資料時是空陣列 `[]` 而非空字串或 `null` | 若當字串處理（`.trim()`、字串比對）會直接壞掉 | 一律用 `Array.isArray` 檢查，`.join('、')` 後再顯示；空陣列視同「無資料」 |
| 11（新增） | `onSales` 有 3 種值，不是純 `Y`/`N` | ✅ 確認：`N`=261、`Y`=80、`UNKNOWN`=3 | 若當布林值 `=== 'Y'` 判斷，`UNKNOWN` 會被誤判為「不售票」 | UI 上把 `UNKNOWN` 顯示為「售票狀態未知」，不要跟 `N` 混為一談 |
| 12（新增） | `imageUrl` 大部分是空字串 | ✅ 確認：94.8%（326/344）為空字串 | 前端若直接當 `<img src>` 會顯示破圖 | 準備預設佔位圖，`imageUrl === ''` 時顯示佔位圖而非空 src |
| 13（新增） | 常設展大量使用「當年度 1/1～12/31」當作 `startDate`/`endDate`，不是真實展期 | ✅ 確認：42.2%（145/344）的 `startDate`/`endDate` 恰好是 `2026/01/01`／`2026/12/31` 這組完全相同的日期，另有 18 筆是 `2026/01/02`／`2026/12/31`。抽查標題多為「○○常設展」「文化館常設展」，明顯是「全年」的年度預設值，不是精確展期 | 這些展覽在「本週」「本月」等區間篩選下**永遠會命中**（因為區間橫跨全年），會讓「本週展覽」清單被大量常設展灌水，稀釋真正限時展覽的能見度 | ✅ **已處理**：新增 `isPermanentExhibition()` 獨立函式與 `isPermanent` 欄位，見 4.2 節；前端預設隱藏常設展，並在結果筆數提示中另外標示常設展數量 |
| 14（新增） | 從 `location` 抽行政區（鄉/鎮/市/區）時，貪婪比對正則會抽錯 | ✅ 確認且已修正：用貪婪比對 `[一-鿿]{1,6}[鄉鎮市區]` 測試全部 344 筆時，`"花蓮縣壽豐鄉市場1號"` 會被誤判成行政區「壽豐鄉市」（把「市場」的「市」也吃進去）。改成非貪婪 `[一-鿿]{1,6}?[鄉鎮市區]`（抽到第一個鄉/鎮/市/區字尾就停）後，全樣本重新驗證沒有再出現這種錯誤 | 行政區篩選選單會出現不存在的假行政區選項，使用者選了會篩不到任何資料 | ✅ **已處理**：`extractDistrict()` 已改用非貪婪正則，見 4.3 節 |
| 15（新增） | 純線上展覽仍掛在某個縣市底下，污染地點篩選（issue #2） | ✅ 確認：用標題關鍵字「線上／virtual／雲端(展\|策展\|平台)」比對 344 筆，抓到 6 筆真的是線上展，其中 5 筆共用同一個地址「高雄市鹽埕區019鄰中正四路272號」（應為機構行政地址，被當成純線上展的佔位地點）。驗證過程也抓到 2 個誤判案例：「雲端上的白鷹－熊鷹特展」標題含「雲端」但其實是遊客中心的實體特展（裸比對「雲端」會誤判，已限定要接「展/策展/平台」才算）；用簡介比對「量水器室特展」也會誤判（簡介提到「線上特展」但那其實是首度開放現場參觀的實體展，已改成只比對標題、不比對簡介） | 使用者篩某縣市想找「可以去看」的展覽時，線上展是雜訊，會佔到清單版面 | ✅ **已處理**：新增 `isOnlineExhibition()` 獨立函式（只比對標題）與 `isOnline` 欄位，見 4.4 節；前端比照常設展處理，預設隱藏、可勾選「包含線上展覽」 |

### 4.1 API 回傳範圍結論

> **結論：這支 API 只回傳「當期／未來」的展覽，不是資料庫全部歷史資料。**

依據（以今天 2026/09/07 為基準，逐筆核對 344 筆 `endDate`）：

- `endDate < today`（已結束）：**0 筆（0%）**
- `startDate <= today <= endDate`（進行中）：303 筆（88.1%）
- `startDate > today`（尚未開展）：41 筆（11.9%）
- `startDate` 範圍：最早 `2024/01/19`，最晚 `2027/01/01`
- `endDate` 範圍：最早 `2026/09/11`，最晚 `2035/12/31`

換句話說，即使某展覽的 `startDate` 早到 2024 年（多半是常設展的起始日），API 依然只回傳那些 `endDate` 還沒過期的紀錄——**伺服器端已經幫忙濾掉過期活動**，前端不需要再另外請求「近期」或「未來」的參數，也代表這支 API **不適合拿來查詢已結束的歷史展覽**。

**另一個重要發現：日期資料裡有大量「年度預設值」，不是精確展期。**

- 42.2%（145/344）的 `startDate`/`endDate` 完全等於 `2026/01/01` / `2026/12/31`
- 加上 `2026/01/02` / `2026/12/31` 這組（18 筆），合計 **47.4%（163/344）** 用的是「整個 2026 年」當展期
- 抽查這些紀錄的標題，多數是「○○常設展」「○○文化館常設展」，可判斷這是常設展在系統裡沒有真實結束日、只好用「當年度全年」當佔位區間，並非展覽真的只在 2026 年開放（例如奇幻自然常設展的 `endDate` 實際填的是 `2034/12/31`，代表這欄位本身可信度因展覽而異）

這代表：用「本週」「本月」做時間篩選時，這 47% 的常設展會**永遠出現**在結果中（因為區間橫跨全年），屬於預期內的資料特性、不是程式錯誤。第一版前端先照實呈現，不特別區分常設展／限時展。

### 4.1.1 統計摘要（sample.json，N=344）

| 統計項目 | 結果 |
|---|---|
| 總筆數（活動） | 344 |
| 總筆數（攤平後場次，`showInfo` 展開） | 344（每筆活動恰好 1 場次，見第 3 節） |
| `UID` 重複數 | 0（344 個皆唯一） |
| 有效經緯度（非 null、範圍合理、非 0）比例 | 33.4%（115/344） |
| 無經緯度（`null`）比例 | 66.6%（229/344） |
| 縣市可由 `location` 正則抽出的比例 | 95.6%（329/344） |
| 縣市抽不出來、且同時無經緯度（真正資料缺口）| 4.4%（15/344） |
| `startDate` 格式樣態 | 100% 為 `YYYY/MM/DD`，無異常格式、無空值 |
| `endDate` 格式樣態 | 100% 為 `YYYY/MM/DD`，無異常格式、無空值 |
| `endDate < startDate` 異常筆數 | 0 |
| `endDate < today`（2026/09/07）異常筆數 | 0（見 4.1「API 回傳範圍結論」） |
| `showInfo.time` / `endTime` 格式樣態 | 100% 為 `YYYY/MM/DD HH:mm:ss` |
| 展期天數（`endDate` - `startDate`）最短／最長／平均 | 2 天／3999 天／約 274 天 |

**`startDate` 月份分布**（344 筆）：

| 年-月 | 筆數 | 年-月 | 筆數 |
|---|---|---|---|
| 2024-01 | 1 | 2026-07 | 15 |
| 2025-09 | 1 | 2026-08 | 16 |
| 2025-10 | 1 | 2026-09 | 25 |
| 2026-01 | 181 ⚠️ | 2026-10 | 10 |
| 2026-02 | 4 | 2026-11 | 7 |
| 2026-03 | 8 | 2026-12 | 5 |
| 2026-04 | 22 | 2027-01 | 5 |
| 2026-05 | 16 | | |
| 2026-06 | 27 | | |

> ⚠️ `2026-01` 有 181 筆（52.6%），是全表最大異常值，主要成因就是上述「常設展用 `2026/01/01` 當年度預設起始日」（159 筆 `startDate` 恰為 `2026/01/01`）。**這不是資料抓取錯誤，是來源資料本身的特性。**

**`endDate` 月份分布**（344 筆）：

| 年-月 | 筆數 | 年-月 | 筆數 |
|---|---|---|---|
| 2026-09 | 47 | 2027-01 | 5 |
| 2026-10 | 44 | 2027-05 | 1 |
| 2026-11 | 25 | 2028-12 | 1 |
| 2026-12 | 218 ⚠️ | 2030-12 | 1 |
| | | 2034-12 / 2035-12 | 各 1 |

> ⚠️ `2026-12` 有 218 筆（63.4%），同樣主因是「常設展用 `2026/12/31` 當年度預設結束日」（203 筆 `endDate` 恰為 `2026/12/31`）。2028～2035 的極端值（4 筆，1.2%）則是少數博物館真正填了「常設展直到某年」的長期規劃日期。

**各縣市分布**（依 `location` 正則抽出，共 329 筆有結果；由高到低）：

| 縣市 | 筆數 | 縣市 | 筆數 |
|---|---|---|---|
| 高雄市 | 66 | 屏東縣 | 8 |
| 彰化縣 | 53 | 澎湖縣 | 8 |
| 臺北市 | 33 | 新竹縣 | 5 |
| 臺南市 | 31 | 雲林縣 | 3 |
| 南投縣 | 29 | 基隆市 | 2 |
| 桃園市 | 22 | 宜蘭縣 | 2 |
| 臺中市 | 20 | 嘉義縣 | 2 |
| 新北市 | 17 | 花蓮縣 | 1 |
| 苗栗縣 | 16 | 新竹市 | 1 |
| 金門縣 | 10 | | |

> 備註：樣本中縣市一律使用「臺」的正體字（臺北市／臺中市／臺南市），完全沒有出現「台」的俗體字寫法；正則需同時涵蓋兩種寫法以防未來資料混用。嘉義市、台東縣、連江縣在本樣本中無資料（0 筆）。

### 4.2 常設展（`isPermanent`）判斷規則

來源資料**沒有**任何欄位標示「是否為常設展」，只能從標題文字和日期形狀去猜，屬於啟發式判斷、不是資料源提供的事實欄位。實作在 `scripts/build-events.mjs` 的 `isPermanentExhibition()` 函式，三個訊號**任一成立**就判定為常設展：

1. **標題關鍵字**：標題含「常設」或「常態」（例如「○○常設展」）
2. **年度佔位日期**：展期的月/日剛好落在年度頭尾——起始為 `1/1` 或 `1/2`，結束為 `12/30` 或 `12/31`，**不限定特定年份**。（原本的規則只抓「某一年 1/1~12/31」的精確配對，樣本裡另外還有 `1/2` 起、`12/30` 迄的變形寫法，比對特定年份也不必要，所以放寬成「比對月/日、容差 1 天」）
3. **展期超長**：起訖日相差超過 730 天（2 年），日期沒有卡在年度邊界、標題也沒關鍵字，但展期長達數年的，實務上也是長期／常設展

三個訊號在 344 筆樣本中的判定結果：

| 訊號 | 命中筆數（含與其他訊號重疊） | 只靠這個訊號才抓到的筆數 |
|---|---|---|
| 標題關鍵字（常設／常態） | 93 | 7 |
| 年度佔位日期 | — | 80 |
| 展期 > 730 天 | — | 1 |
| **合計判定為常設展** | **175（50.9%）** | — |

> ⚠️ 這條規則**不可能 100% 準確**。例如樣本中的「大英博物館鉅獻《埃及之王：法老》」（`2026/01/29~2026/12/31`，展期約 11 個月）是真正的限時特展，只是剛好卡到年底，規則正確地沒有把它判定為常設展；但反過來，靠日期形狀本身無法完全排除「真的很長但仍是限時」的邊界案例（例如展期剛好落在 731 天以上的限時特展會被誤判）。這是可接受的取捨，之後若要更準，需要資料源本身提供明確欄位，光靠現有欄位猜不出來。

### 4.3 行政區（`district`）欄位抽取規則

跟 `isPermanent` 一樣，來源資料**沒有**行政區欄位，只能從 `location` 地址字串用正則抽（見 issue #4「加入行政區篩選」）。實作在 `scripts/build-events.mjs`：

1. 先用 `findLastCountyMatch()` 找出 `location` 字串裡**最後一次**出現的縣市名稱位置。用「最後一次」而不是「第一次」，是因為樣本裡有「臺中市40453 臺中市北區館前路一號」這種縣市名重複、中間夾著郵遞區號的寫法（見 4-6），只有取最後一次才能正確跳過郵遞區號本身。這個函式同時也拿來取代原本 `extractCounty()` 的第一次比對，兩者統一用同一個基準。
2. 從縣市名稱**之後**的文字，用非貪婪正則 `[一-鿿]{1,6}?[鄉鎮市區]` 抽出行政區——非貪婪是必要的，見上表風險 #14 的貪婪比對錯誤案例。
3. 找不到縣市時（4.4% 的地址沒有縣市前綴，見 4-6），直接對整個 `location` 字串比對，一樣抽得出行政區（只是沒有對應的縣市）。

**實測涵蓋率**（344 筆場次）：

| 統計項目 | 結果 |
|---|---|
| 縣市＋行政區皆有 | 324（94.2%） |
| 只有行政區、沒有縣市（地址沒寫縣市前綴，見 4-6） | 13（3.8%） |
| 只有縣市、沒有行政區（地址本身沒寫到行政區，如「臺北市南海路四十七號」，是來源資料缺漏，不是正則問題） | 5（1.5%） |
| 縣市、行政區都抽不出來 | 2（0.6%） |
| **至少抽到行政區** | **337（98.0%）** |

**縣市→行政區分布**（324 筆縣市+行政區皆有）：全部 19 個有資料的縣市底下共 101 組不重複的（縣市, 行政區）組合，例如高雄市 16 個行政區（鹽埕區 18 筆最多）、彰化縣 15 個、臺北市 8 個。細節見 `events.json`。

### 4.4 線上展（`isOnline`）判斷規則

跟 `isPermanent`、`district` 一樣，來源資料**沒有**欄位標示「是否為純線上展」，只能從標題文字猜（見 issue #2「線上展覽污染地點篩選」）。實作在 `scripts/build-events.mjs` 的 `isOnlineExhibition()` 函式：

```
標題含「線上」或「virtual」，或標題含「雲端展」/「雲端策展」/「雲端平台」
```

**只比對標題，不比對簡介**——這是驗證時刻意的決定，不是照 issue 原本「標題或簡介」的建議，因為實測比對簡介會誤判：《謎知的冒險─開箱百年前的量水黑科技》量水器室特展的簡介提到「文資網線上特展」，但那其實是一檔**首度開放現場參觀**的實體特展，線上只是附加的導覽形式，不該被歸類成線上展；標題比對在樣本裡沒有這個問題。

**「雲端」限定要接「展/策展/平台」**，不能裸比對「雲端」兩個字——「雲端上的白鷹－熊鷹特展」標題含「雲端」，但那是南投水里遊客中心一檔有標本、3D 列印模型可以摸的**實體**特展，「雲端」在這裡是「翱翔雲端」的字面意思，跟線上平台無關。

**實測結果**（344 筆）：符合規則的有 **6 筆**，全部人工核對過確實是純線上展，其中 5 筆共用同一個地址「高雄市鹽埕區019鄰中正四路272號」（應是機構的行政地址，被拿來當這些純線上展的佔位地點，正好對應 issue 描述的「污染地點篩選」現象）。6 筆裡有 3 筆同時也被判定為 `isPermanent`，兩個欄位互相獨立、不衝突，一檔展覽可以同時是常設展又是線上展。

> ⚠️ 跟 `isPermanent` 一樣，這條規則**不可能 100% 準確**，只比對標題代表「簡介才提到是線上展、標題沒寫」的案例會被漏掉，但目前樣本沒有這種情況，寧可漏抓也不要抓錯。

---

## 5. 建議架構

```
[GitHub Actions 每日排程]
        ↓
  抓取 API → 清洗 → 攤平 → 輸出 events.json（靜態檔）
        ↓
  [前端] 載入 events.json → 依地點/日期在瀏覽器端篩選
        ↓
  GitHub Pages 免費託管
```

這個做法一次解決 CORS、速度、以及「不想管後端」三件事。前端不需要伺服器，純靜態即可。

---

## 6. 篩選邏輯定義

**時間篩選** — 判斷活動期間與查詢期間是否重疊：

```
活動符合 = (startDate <= 查詢結束日) AND (endDate >= 查詢開始日)
```

常用預設選項：今天／本週／本月／自訂區間。

**地點篩選** — 三種模式：

1. **縣市** — 從 `location` 抽出縣市字串比對。無座標的活動也適用，實測覆蓋率 95.6%（見 4.1）。
2. **行政區**（issue #4 新增）— 從 `location` 抽出縣市之後的鄉/鎮/市/區，實測覆蓋率 98.0%（見 4.3）。UI 上採**串聯選單**，掛在縣市之下（先選縣市才能選行政區），因為行政區名稱在不同縣市會重複（例如「東區」臺中、臺南、新竹市都有），扁平選單會有歧義。
3. **距離** — 以使用者座標為中心，用 Haversine 公式算距離並依半徑過濾。精準，但實測僅 33.4% 的場次有座標，缺座標的活動會被排除。目前**尚未實作**（v1 範圍排除地圖／距離功能，因為缺座標比例太高，地圖會誤導使用者，見卡片列表 vs 地圖的決策紀錄）。

縣市＋行政區篩選是同一個場次（`showInfo` 項目）要**同時**符合兩個條件，不是分別在不同場次上各自符合——不然像「東區」這種在多個縣市都有的行政區名稱，會誤配到別的縣市去。一檔展覽若有多個場次分屬不同縣市/行政區，卡片上會列出所有場次，用 ✓ 標記符合篩選條件的場次、其餘場次淡化顯示（不隱藏），讓使用者看得出「是哪一場讓這張卡片被篩出來」，同時仍保留展覽其他場次的資訊。

另有 4.4%（15/344）的資料連縣市都抽不出來，兩種文字篩選方式都無法定位，UI 上應誠實標示「地點未知」而非隱藏或誤分類。

---

## 7. 下一步待辦

- [x] 實際抓一次 API，存成 `sample.json`（2026/09/07，N=344，`category=6`；因本環境出口網路政策擋掉 `cloud.culture.tw`，本次由使用者於環境外抓取後提供檔案，非本 session 直接連線取得）
- [x] 驗證上表第 3、4、6、8 項的實際狀況（見第 4 節，含 3 項額外新發現：欄位拼字/大小寫、unit 欄位為陣列、`onSales` 三值）
- [x] 統計：總筆數、有座標的比例、各縣市分布、日期格式樣態（見 4.1）
- [x] 確認 `showInfo` 的實際巢狀結構與欄位大小寫（見第 2 節；正確欄位名為 `showInfo`，非官方文件的 `showinfo`）
- [x] 依實際結果修正本文件，再開始寫前端

**尚未驗證、留待之後補充：**

- [ ] CORS 標頭是否真的缺失（本次抓取非瀏覽器端，未觸發 CORS 檢查）
- [ ] `category=all` 或其他 category 是否會出現 `showInfo` 陣列長度 > 1 的巡迴展案例
- [ ] 跨日重新抓取後 `UID` 是否穩定不變
- [ ] `endDate < today` 的過期活動實際佔比

---

## 8. 北美館（TFAM）資料源整合（issue #5）

### 8.1 背景

文化部展覽 API（第 1～7 節的主要資料源）幾乎沒有大型場館的資料——用「北美館」「臺北市立美術館」「國家檔案館」「當代美術館」等關鍵字搜 `sample.json`（N=344）結構化欄位，命中數全部是 0；「故宮」「國美館」也只各 1 筆。北美館、國家檔案館、臺北市當代藝術館這三個完全沒有資料。這個 sandbox 環境連不上 `cloud.culture.tw`／`data.taipei`／`www.tfam.museum` 任何一個網域（出口網路政策擋掉，見前面章節的多次記錄），所以無法在本 session 直接查證 `category=15`／`category=all` 是否涵蓋這些場館，也無法直接呼叫 data.taipei 的 API，這兩項留在第 7 節「尚未驗證」清單。

**決定採用的方案**：接北美館在 data.taipei 上的展覽資訊 API（[dataset 1700a7e6-3d27-47f9-89d9-1811c9f7489c](https://data.taipei/api/v1/dataset/1700a7e6-3d27-47f9-89d9-1811c9f7489c?scope=resourceAquire)），缺的欄位用人工整理的 override 檔案補。

### 8.2 data.taipei API 欄位對應

| data.taipei 欄位 | 對應到 | 備註 |
|---|---|---|
| `title` | `title` | |
| `內容` | `description` | 品質好，直接用，不像文化部的 `descriptionFilterHtml` 需要消毒/摘要 |
| `發布單位` | `showUnit` | |
| `countycode`（固定 `63000`） | — | 不需要另外處理，county/district 交給 `extractCounty`/`extractDistrict` 從 override 給的 `location` 算，跟文化部資料走同一套邏輯 |
| （無） | `startDate`／`endDate`／`imageUrl`／`location` | **API 缺這四個欄位**，這是這次要補的重點 |
| （無） | UID | API 沒有唯一識別碼，`uid` 由 `title` 算穩定雜湊產生（`tfam-<hash>`），同一標題每次 build 結果一致 |

### 8.3 `data/tfam-overrides.json` 與合併邏輯

`scripts/build-events.mjs` 新增 `loadTfamEvents()`：

1. 讀 `data/tfam-overrides.json`（**必要**，人工整理的補值清單，每筆用 `title` 當比對 key）
2. 讀 `data/tfam-raw.json`（**選用**——data.taipei API 的原始回傳，目前**還沒有抓取流程**，檔案不存在時當作空陣列，不會讓腳本壞掉）
3. 依 `title` 把兩邊合併：`description`／`showUnit` 從 raw 拿（沒有 raw 資料就是空字串）；`startDate`／`endDate`／`imageUrl`／`location`／`locationName`／`latitude`／`longitude` 一律從 override 拿
4. `isOnline` 優先用 override 裡明講的值（`"isOnline" in override`），沒講才照一般標題關鍵字規則猜——這是因為 #2「TFAM Net.Open」標題沒有「線上」兩個字，一般規則抓不到，需要人工標記
5. `isPermanent` 照一般規則算，不開放 override（目前沒有需要）
6. 每筆額外加 `source: "tfam"`，方便之後區分資料來源、除錯

`overrides` 裡即使某個 `title`在 `tfam-raw.json`（或未來真正抓到的 API 回傳）裡完全找不到，也會照樣產生一筆事件——只是 `description`/`showUnit` 是空字串。這是刻意設計，因為 overrides 裡有 2 筆（王雅慧、調）是北美館官網有、但 data.taipei API 沒收錄的展覽，整筆資料本來就要靠人工補，不是「補值」而是「新增」。

輸出時 `events = [...文化部事件, ...TFAM事件]`，攤平成同一個陣列，前端不用改——`index.html` 本來就是把 `events.json`當成單一陣列處理。

### 8.4 目前 `data/tfam-overrides.json` 的 7 筆內容

資料來源：WebSearch 查詢北美館官網及相關報導整理（這個 sandbox 連不上 `www.tfam.museum` 直接查證，見前面章節），已與使用者逐筆核對確認。

| 展覽 | 展期 | 地點 | 備註 |
|---|---|---|---|
| 超現實主義：對話中的世界 | 2026/04/25～2026/08/30 | 北美館固定地址 | ⚠️ 見 8.5 |
| 北美館開放網絡計畫 TFAM Net.Open｜消失的反動 | 2025/09/19～2026/08/31 | **無**（`location`/`county`/`district`/經緯度皆為空／`null`） | `isOnline: true`（人工標記，純線上展，標題沒有「線上」關鍵字，一般規則抓不到）；⚠️ 見 8.5 |
| 造公園 | 2026/03/28～2026/08/30 | 北美館固定地址 | ⚠️ 見 8.5 |
| 共感：存在的節奏 | 2026/05/09～2026/09/20 | 北美館固定地址 | |
| 鬱卒的平面：李亦凡／第61屆威尼斯國際美術雙年展台灣館 | 2026/05/09～2026/11/22 | `location: "義大利威尼斯"`，經緯度 `null` | 北美館主辦但展出地點在義大利威尼斯 Palazzo delle Prigioni，**沒有套用北美館的台北座標**，避免產生錯誤地理資料 |
| 王雅慧：旅行者 | 2026/09/12～2027/01/03 | 北美館固定地址 | data.taipei API 沒有這筆，整筆資料由 override 直接提供 |
| 調：心境與聲景之間 | 2026/07/04～2026/09/27 | 北美館固定地址 | 同上，API 沒有這筆 |

北美館固定地址：`location: "臺北市中山區中山北路三段181號"`、`locationName: "臺北市立美術館"`、`latitude: 25.072656`、`longitude: 121.524559`（套用後 `extractCounty`/`extractDistrict` 正確算出「臺北市」「中山區」，已驗證）。

**圖片網址目前全部留空**（`imageUrl: ""` → 正規化成 `null`，前端會顯示佔位圖）——WebSearch 查不到實際圖片檔案網址（它只回傳文字摘要，沒辦法像直接開網頁一樣讀 `<img>`／`og:image` 標籤），已在官網專頁列出（見對話紀錄），之後有網路權限的環境可以直接抓。

### 8.5 已知的資料品質問題

- **3 筆展期已早於本次整理的日期（2026/09/11）結束**：超現實主義（8/30 迄）、造公園（8/30 迄）、Net.Open（8/31 迄）。這些日期是用 WebSearch 查到的，可能是文章寫作當下的資訊、之後可能有展期延長但沒查到，**使用者已確認保留這幾筆**，但這代表：(a) 這幾筆展覽在「今天／本週／本月」篩選下不會出現（時間篩選邏輯正確運作），只有在「不限時間」或涵蓋過去日期的自訂區間才看得到；(b) 這是文化部資料源目前沒有的情況——文化部 API 本身只回傳未過期的展覽（見 4.1），TFAM 這批用人工 override 補的資料則可能包含已過期項目，未來重新核對這幾筆的真實展期時要注意。
- **卡片上的地點顯示文字對這兩筆特例不夠精確**：Net.Open（純線上展）目前顯示「📍 （縣市未知）」，威尼斯那筆顯示「📍 威尼斯雙年展台灣館（縣市未知）」——`(縣市未知)` 這個字眼原本是給「資料缺漏」用的（見 4-6 的 15 筆真缺地址資料），套在「故意設計成沒有台灣縣市」的這兩筆上語意不太精準，但不影響篩選邏輯正確性（不會被誤配到任何台灣縣市底下）。純粹是顯示文字可以再修飾的小地方，這次沒有動，先如實記錄。
- **`tfam-raw.json`（data.taipei API 的原始回傳）還沒有抓取流程**，目前 7 筆事件的 `description`／`showUnit` 全部是空字串。已經用假資料驗證過合併邏輯本身沒問題（見 commit 訊息），但要在正式資料裡看到真的簡介/發布單位文字，還需要之後接上實際抓取 data.taipei API 的步驟（可能是 GitHub Actions，見第 5 節架構）。
